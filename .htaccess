<IfModule mod_rewrite.c>
    RewriteEngine On

    # Se o arquivo existe na raiz (ex: setup.php), executa diretamente
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>

